# ── Core FastAPI / server ─────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1
pydantic>=2.6.0

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.4.24

# ── HTTP client (Ollama) ──────────────────────────────────────────────────────
httpx>=0.27.0

# ── PDF generation ────────────────────────────────────────────────────────────
reportlab>=4.1.0

# ── CORS / middleware ─────────────────────────────────────────────────────────
# (included with FastAPI/Starlette – no separate package needed)
